{
  "name": "js-vitest-project",
  "version": "1.0.0",
  "description": "Simple JavaScript project with Vitest",
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "create-commits": "node scripts/create-commits.js"
  },
  "devDependencies": {
    "vitest": "^1.3.1",
    "@vitest/coverage-v8": "^1.3.1"
  }
} 